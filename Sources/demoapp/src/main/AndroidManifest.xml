<?xml version="1.0" encoding="utf-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required for Live Update notifications (promoted ongoing notifications) -->
    <uses-permission android:name="android.permission.POST_PROMOTED_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Demoapp"

        tools:targetApi="35">

        <meta-data android:name="com.pushwoosh.appid" android:value="@string/pushwoosh_app_id" />
        <meta-data android:name="com.pushwoosh.senderid" android:value="@string/fcm_sender_id" />
        <meta-data android:name="com.pushwoosh.apitoken" android:value="@string/pushwoosh_api_token"/>
        <meta-data android:name="com.pushwoosh.rich_media_type" android:value="Modal" />
        <meta-data android:name="com.pushwoosh.log_level" android:value="NOISE" />
        <meta-data android:name="com.pushwoosh.multi_notification_mode" android:value="true" />

        <meta-data
            android:name="com.pushwoosh.notification_service_extension"
            android:value="com.pushwoosh.demoapp.NotificationServiceExtensionDemo" />

        <meta-data
            android:name="com.pushwoosh.CALL_EVENT_LISTENER"
            android:value="com.pushwoosh.demoapp.ui.DemoCallEventListener" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.CallCancelledActivity"
            android:theme="@style/Theme.Demoapp"
            android:excludeFromRecents="true"
            android:exported="false" />

        <receiver
            android:name=".liveupdate.LiveUpdateActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.pushwoosh.demoapp.ACTION_CANCEL_ORDER" />
                <action android:name="com.pushwoosh.demoapp.ACTION_ADD_TIP" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
